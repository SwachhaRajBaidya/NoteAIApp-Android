Do a clean install each time in order to avoid crashes. Delete the apk-debug.apk too.
Delete the installed app first then delete the apk then download new version and then install.

DO THIS AND APP WILL NEVER CRASH

LATEST VERSION: v0.1.6-alpha(pro)

Uses Gemeni now





Totally Unrelated: You are using latest version of love2d to compile code written for love2d v0.9.2. Of course it will show errors. :D
                   If you want to run it in newer version of love2d
                      - Firstly, the audio:setLooping(audio) has been changed to audio:setLooping(boolean)
                      - Secondly, divide all the color values with /255 in order to get the actual colors
                      - Thirdly, there are different window modes in the newer version than the older version change that to get true fullscreen
                      - I don't remember other. But I have already made changes and updated it and it is working perfectly. :D
